#NumPy program to multiply two given arrays of the same size element-by-element
import numpy as np
array1 = np.array([1, 2, 3, 4])
array2 = np.array([5, 6, 7, 8])
result = np.multiply(array1, array2)
print("Result of element-wise multiplication of the two arrays:")
print(result)
